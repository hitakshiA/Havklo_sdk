name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

jobs:
  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          release_name: Release ${{ github.ref_name }}
          draft: false
          prerelease: ${{ contains(github.ref_name, 'alpha') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'rc') }}

  build-wasm:
    name: Build WASM Package
    runs-on: ubuntu-latest
    needs: create-release
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-action@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Build WASM
        run: |
          cd crates/kraken-wasm
          wasm-pack build --target web --out-dir ../../pkg

      - name: Create WASM archive
        run: |
          cd pkg
          zip -r ../kraken-wasm-${{ github.ref_name }}.zip .

      - name: Upload WASM Package
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          asset_path: kraken-wasm-${{ github.ref_name }}.zip
          asset_name: kraken-wasm-${{ github.ref_name }}.zip
          asset_content_type: application/zip

  # Optional: Publish to crates.io
  # Uncomment when ready to publish
  # publish:
  #   name: Publish to crates.io
  #   runs-on: ubuntu-latest
  #   needs: [build-wasm]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: dtolnay/rust-action@stable
  #     - name: Publish crates
  #       env:
  #         CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
  #       run: |
  #         cargo publish -p kraken-types
  #         cargo publish -p kraken-book
  #         cargo publish -p kraken-ws
  #         cargo publish -p kraken-sdk
